export interface TextTag {
    bold?: boolean;

    fs10?: boolean;
    fs12?: boolean;
    fs14?: boolean;
    fs16?: boolean;
    fs18?: boolean;
    fs20?: boolean;
    fs22?: boolean;
    fs24?: boolean;

    color?: string;

    center?: boolean;

    pointer?: boolean;
}